#include <iostream>
using namespace std;

int main() {
    char a;
    cout << "Vvedit cyfru: ";
    cin >> a;

    cout << "-" << a << "-" << a << "-" << a << endl;
    cout << a << "l" << a << "l" << a << endl;
    cout << "-" << a << "-" << a << "-" << a << "," << endl;

    return 0;
}
